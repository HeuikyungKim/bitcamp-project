//자바관련 빌드 명령을 사용하기 위해 
apply plugin: 'java'

apply plugin: 'eclipse'

/* Gradle의 기본폴더구조는 다음과 같다.


*/
//만약 소스 폴더의 구조를 변경하고 싶다면 다음 설정을 추가하라

sourceSets {
    main {
       java {
            srcDir 'src'
        }
        resources {
            srcDir 'src'
        }
    }
}